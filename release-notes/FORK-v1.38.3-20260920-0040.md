# Reasonix Fork 桌面版 v1.38.3-20260920-0040 — Release Notes（UI 布局修复）

> **增量基线**：上一包 `v1.38.3-20260919-2310`。内容 = 2310 全部 + 两处设置面板布局修复。

## 修复：设置面板两处布局问题

1. **会话存储**：说明与「阶段 / 已知风险」文案改为**按钮下方纵向排列**，
   长文案完整换行显示，不再挤压截断（此前 hint 单行省略号截断 + 多段挤在按钮旁）。
2. **写目录三层**：项目 / 会话 / 全局三层**纵向排列**（此前前两层并排一行），
   每层含完整说明与目录列表，层级从窄到宽自上而下。

## 保留内容

2310 的全部内容：内存膨胀根因修复（snapshot 有界重建，96MiB 预算）、
任务 171 恢复、性能监控 + heap profile、任务 170 会话工具、首载提速与预取、
引导消息可见性等。
