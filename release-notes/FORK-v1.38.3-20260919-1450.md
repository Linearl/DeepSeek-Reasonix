# Reasonix Fork 桌面版 v1.38.3-20260919-1450 — Release Notes（hotfix：内存回归回退）

> **增量基线**：上一包 **`v1.38.3-20260919-1055`**。本包为内存回归的回退修复包，
> **建议所有用户从 1300/0426/1055 直接升级到本包**。

## 修复：回退引入内存回归的改动（任务 171 实施暂缓）

**症状**：1300/0426/1055 三个包在工作时段出现严重内存膨胀
（进程内存 14.6-20GB，磁盘持续写入），回退到 1250 或更早版本后恢复正常。

**二分定位**：1250 → 1300 的唯一代码差异是任务 171 的实施（9 文件），
引入点锁定。本包将该改动整体回退（`0ac4c14f0`），内存行为回到 1250 水平。

**随包保留**（不受回退影响）：会话管理工具（rename/move）、关 tab 不阻塞、
测试防线恢复（13→0）、启动首载提速与标签预取、预取冗余修复、引导消息
可见性、OpenCode 修复、四档切换——1250 之后除 171 实施外的全部内容。

## 任务 171 的后续

storm breaker 误伤修复与 pending-handoff 落盘**暂缓**——已定位实施引入
内存回归，待用 heap profile 精确定位泄漏点并修复后重新实施（commit
`29deeb6e7` 保留在历史中可随时恢复）。

## 附带修复

- 出包脚本无参时自动生成时间戳版本号（根治「version is required」）
- 预取冗余重读清零（87.7% → 0，切换时快时慢的修复）

**验证**：go build 双模块 / tsc / desktop 编译全绿。
