# Reasonix Fork 桌面版 v1.38.3-20260920-1130 — Release Notes（重放上限放宽）

> **增量基线**：上一包 `v1.38.3-20260920-1115`（本次不换内容只补一个关键修复）。

## 修复：超大会话每次切换都卡 8 秒（重放上限撞墙）

**实测定位（1115 装机后，用户切「需求开发」会话体感 8 秒+）**：该会话
events.jsonl 已达 **467MB / 100,001 条事件记录**，而重放硬上限是
`maxRecords=100_000`——**每次切回都重放到第 100001 条才失败**（467MB 已
读+解析 99.9%），然后整次重放作废重来 ⇒ 稳定 8 秒级卡顿，且「偶尔快」
（小会话不撞墙）。

**修复**：重放三上限（records/messages/collection items）100k → **400k**，
注释记录实例依据；字节硬顶 **1GiB 不变**（真实内存防线：防损坏日志耗尽
RAM），小文件膨胀成巨图的防护仍在。

**适用**：仅 v3 读模式（v3 重放路径）；v4 读模式的对应优化已由 187（索引层）
覆盖，内容层缓存视 1115/1130 实测再定。

## 验证

build 全绿；agent 重放/限制测试族全绿（既有测试用自设小上限，不受常量放宽影响）。

## 追加（f105788ab，同包）：重放撞限不再冻结会话（任务 193 自救）

`SessionReplayLimitError`（records/messages 超限）时不再直接拒绝——先尝试
**从日志尾部的最后一条 replace 全量快照重放**（连同其后的增量 appends，经同一
内存上限校验）；自救成功即可打开会话（不再冻结），失败维持原拒绝语义。
467MB/10 万+条会话每次切回 8 秒卡顿的根治（上限放宽 + 撞限自救双保险）；
Save 路径的 oversized 自动 compact 在重放恢复后重新生效。
