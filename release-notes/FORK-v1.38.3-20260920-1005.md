# Reasonix Fork 桌面版 v1.38.3-20260920-1005 — Release Notes（实验室改版）

> **增量基线**：上一包 `v1.38.3-20260920-0040`。内容 = 0040 全部 + 实验室改版（任务 185）。

## 改版：「实验特性」→「实验室」，按领域分组（方案 A+C）

1. **改名**：设置主入口与页内标题改为「实验室」（zh/zh-TW「實驗室」/en「Lab」），
   副标题强调「默认关闭、逐档灰度、可随时回退」。
2. **左栏分组标题**：14 个特性按 6 领域分组渲染——会话与存储 / 界面与交互 /
   自动化与无人值守 / 文件与缓存 / 观测与工具 / 反馈与协作；组标题灰色小字，
   开关状态圆点保留。
3. **顶部领域过滤**：rail 上方一排过滤 chip（全部 + 6 组，带计数），点击过滤左栏，
   空组自动隐藏，「全部」恢复完整列表。
4. 详情区行为不变（分组不影响右侧详情与重启横幅）。

## 保留内容

0040 的全部内容（会话存储/写目录三层布局修复、重启并更新按钮 guard 顺序修复、
内存膨胀根因修复、任务 171 恢复、性能监控 + heap profile 等）。
